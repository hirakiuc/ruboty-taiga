require "spec_helper"

RSpec.describe Ruboty::Taiga do
  it "has a version number" do
    expect(Ruboty::Taiga::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
